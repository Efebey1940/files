{
  "name": "Ubuntu 22.04",
  "image": "ubuntu:22.04",
  "runArgs": [
    "--cap-add=ALL",
    "--security-opt", "seccomp=unconfined",
    "--tmpfs", "/tmp",
    "--tmpfs", "/run",
    "--tmpfs", "/run/lock",
    "--volume", "/sys/fs/cgroup:/sys/fs/cgroup:ro",
    "--volume", "/dev:/dev",
    "--volume", "/:/mnt"
  ],
  "mounts": [
    "source=/var/lib/docker,target=/var/lib/docker,type=bind",
    "source=/sys/fs/cgroup,target=/sys/fs/cgroup,type=bind,consistency=cached"
  ],
  "postStartCommand": "apt update && apt install systemctl docker.io wget curl git sudo docker-compose -y && hostname EfeVPS",
  "remoteUser": "root",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode-remote.remote-containers"
      ]
    }
  }
}
